// A minified bundle: discovery skips *.min.js, so this duplicate `greet`/`shout` is never
// parsed and can never make the real exports ambiguous.
export function greet(){return 0}export function shout(){return 0}
