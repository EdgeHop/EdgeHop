// ANTI-CASE: a minified vendor bundle. Discovery skips *.min.js, so this duplicate
// showAlert export must NEVER be parsed — if it were, showAlert would be ambiguous and the
// precise match would (correctly) drop the edge, failing the fixture test.
export function showAlert(){/* minified third-party noise */}
